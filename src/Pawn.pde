class Pawn {
  int x;
  int y;
  int points = 1;
  boolean displayed;
  boolean white;
  
  Pawn(int x, int y, boolean displayed, boolean white) {
    this.x = x;
    this.y = y;
    this.displayed = displayed;
    this.white = white;
  }
  
  void display() {
    
  }
  
  void move() {
    
  }
}
